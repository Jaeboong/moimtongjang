{
  "name": "meeting-account-manager",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "ci": "npm run test -w server && npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}